# MemPalace (local AI memory - never commit)
.mempalace/
entities.json

# Mapa mental Regente v3 — PDFs/imagens fonte (nao commitar; 100MB+)
regente_sass/
amigao_regente/
# Legislação bruta fornecida pela sócia (PDFs grandes; ingeridos em legislation_documents)
legislacao/
legislação ms/
legislação mt/
Legislações Regente/
openapi_temp.json
token_temp.txt

# Ambiente Python
venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyre_configuration

# Ambiente — qualquer .env* fica bloqueado, exceto os templates .example.
# (NUNCA cole secrets reais em arquivos .example — eles SÃO commitados.)
.env
.env.*
*.env
!.env.example
!.env.production.example
!_env.example

# IDE
.vscode/
.idea/

# Logs
*.log
api_logs.txt
worker_logs.txt
ops/runtime/

# Hotfix scripts pontuais (efêmeros)
.fix.py

# MemPalace residual (revogado em 23/04 — Sprint Z)
mempalace.yaml

# Node / Frontend
**/node_modules/
**/.next/
**/out/
**/.expo/
**/.turbo/
coverage/
**/*.tsbuildinfo

# Test coverage artifacts
.coverage

# Sprint 0 — outputs gerados pelos scripts de ingestão (text preview é grande)
ops/legislation_preview/
ops/legislation_metadata.json

# Relatórios temporários de agentes (espelho local de /tmp/ no Windows)
tmp_*.md
normas_k3/
!data/normas_k3/
ops/normas_k3_preview/

# agente Claude Code
.claude/
*.docx
!docs/**/*.docx

# Corpus regulatório bruto (PDFs SEMAD etc.) — já ingerido no knowledge_catalog.
# 254 MB + nomes de arquivo > 255 bytes quebram o checkout do git no Linux
# (deploy Render falhava com "File name too long"). Fonte fica fora do git.
docs/base_regulatoria/

# Planilhas de curadoria da Isis — insumo, nao produto. 18 MB de binario
# (incluindo um .md de 16 MB). O que entra versionado e o MANIFESTO extraido
# delas por `scripts/extrair_manifesto.py` (ADR-038).
curadoria_isis/

# Dumps e arquivos empacotados — prevencao, nao reescrita (#301).
#
# O historico ja carrega 110 MB em 8 blobs mortos: frontend.zip (43,4 MB) e 7
# PDFs SEMAD (66,6 MB). O episodio dos PDFs quebrou o clone do Render no Linux
# (commit 6d7eab9). Remover do working tree NAO remove do historico.
#
# A lacuna quase pegou de novo: o backup de 109 MB da Fase 4 apareceria como
# untracked, porque *.dump nao estava listado aqui.
*.dump
*.sql.gz
*.zip
