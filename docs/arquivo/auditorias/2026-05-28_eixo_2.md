# Auditoria Eixo 2 — Fechar MVP1

> **Tipo:** constatação read-only (EXISTE / PARCIAL / FALTA).
> **Data:** 2026-05-28
> **Working dir:** `c:\Users\Administrador\Desktop\Amigao_do_Meio_Ambiente` (clone independente)
> **Branch:** `feat/dashboard-redesign-v2`
> **Escopo:** estado atual no código dos 3 itens do Eixo 2. Sem propor correção, sem priorizar, sem comparar com outros sistemas.

---

## Item 2.1 — Ingestão real de WhatsApp e e-mail

| # | O que checa | Status | Onde no código | Observação factual |
|---|---|---|---|---|
| 1 | Endpoint/webhook para receber mensagem externa (Twilio/Evolution/Z-API/SendGrid/Mailgun/IMAP) | **FALTA** | `app/api/v1/` (28 routers inspecionados) | Nenhuma rota POST recebe payload de provider externo. Os termos `twilio/evolution/zapi/sendgrid/mailgun/imap/whatsapp/webhook` aparecem em 13 arquivos, mas todos em outros contextos: `webhook_tasks.py` é entrega **de saída** de alertas operacionais ([app/workers/webhook_tasks.py:26](app/workers/webhook_tasks.py#L26)); `channel`/`source_channel` são strings de metadado. |
| 2 | `agent_atendimento` processa input não-manual (mensagem recebida)? | **FALTA** | [app/agents/atendimento.py:28-51](app/agents/atendimento.py#L28-L51) | `execute()` lê `self.ctx.metadata.get("description")` e delega a `classify_demand_with_llm`. `source_channel` é passado como string informativa. Não há recebimento/parse de mensagem; trabalha sobre texto já presente no contexto (criado pelo wizard). |
| 3 | Configuração de provider em `config.py`/settings (chaves, números WhatsApp, contas e-mail) | **PARCIAL** | [app/core/config.py:88-113](app/core/config.py#L88-L113) | Existe SMTP **de saída** (`SMTP_HOST/USER/PASSWORD`, default Mailtrap) e `RESEND_API_KEY` (waitlist). LLM keys (`OPENAI/GEMINI/ANTHROPIC`). **Não há** config de WhatsApp, IMAP/inbound, Twilio, Evolution, Z-API, Mailgun ou SendGrid. |
| 4 | `IntakeDraft` permite criação programática (fora do wizard manual)? | **FALTA** | [app/models/intake_draft.py:40](app/models/intake_draft.py#L40); [app/api/v1/intake.py:421-440](app/api/v1/intake.py#L421-L440) | Única instanciação de `IntakeDraft(` no código está em `create_draft`, protegido por `get_current_internal_user` — usa `current_user.tenant_id`/`current_user.id`. Snapshot do wizard em `form_data`. Não há caminho que crie draft a partir de mensagem externa. |
| 5 | Modelo/tabela de "mensagens externas / inbox / thread de conversa" | **PARCIAL** | [app/models/communication.py:8-44](app/models/communication.py#L8-L44) | Existem `CommunicationThread` (cols `channel` = 'whatsapp'/'email'/'internal', `external_id`) e `Message` (cols `external_msg_id`, `status` sent/delivered/read/failed). **Porém** a criação é só manual: `add_message` exige `get_current_internal_user` e grava `sender_id=current_user.id` ([app/api/v1/threads.py:43-66](app/api/v1/threads.py#L43-L66)). Schema preparado para canal externo, sem ingestão que o popule. |
| 6 | Dependência de cliente WhatsApp / parser de e-mail em `requirements.txt` | **FALTA** | `requirements.txt` | Busca por `twilio/evolution/imapclient/mail-parser/emails/sendgrid/mailgun/aiosmtpd/imaplib`: nenhum match. `app/services/email.py` usa `EmailMessage` (stdlib, envio). |

---

## Item 2.2 — Motor de workflow por tipo de demanda

| # | O que checa | Status | Onde no código | Observação factual |
|---|---|---|---|---|
| 7 | Assinatura de `execute()` do `agent_legislacao` e inputs aceitos | **EXISTE** | [app/agents/legislacao.py:98-143](app/agents/legislacao.py#L98-L143) | `execute()` lê de `metadata`/`chain_data`: `query`, `demand_type` (fallback `chain_data["atendimento"]["demand_type"]`), `state`. Se houver `process_id`, enriquece via `_load_process_context()` (demand_type, UF, município, propriedade — [linha 350](app/agents/legislacao.py#L350)). Precondição: ao menos um de `query`/`demand_type`/`process_id`. |
| 8 | Consulta à base regulatória RAG — onde e quais filtros | **EXISTE** | [app/agents/legislacao.py:251-348](app/agents/legislacao.py#L251-L348) | Dois caminhos: (a) RAG semântico via `knowledge_catalog.search(...)` com filtro `source_type="legislation"` e `uf` (com fallback global sem UF) — [linha 273](app/agents/legislacao.py#L273); (b) dump por metadados via `search_legislation(uf=, demand_type=, ...)` — [linha 335](app/agents/legislacao.py#L335). O `demand_type` no RAG entra **na string de query** (concatenado), não como coluna filtrada. |
| 9 | Formato e persistência do output do `agent_legislacao` | **EXISTE** | [app/agents/legislacao.py:225-241](app/agents/legislacao.py#L225-L241); `app/schemas/stage_output.py` | Emite `EnquadramentoRegulatorioContent` (StageOutputContent derivado): `caminho_regulatorio`, `orgao_competente`, `etapas`, `legislacao_aplicavel`, `riscos`, `normas_estaduais`, `prazos_legais`, `sources`, `chunks_referenced`. `requires_review=True` sempre. Persistência segue o padrão `BaseAgent.run()`/AIJob (não há coluna dedicada no Process). |
| 10 | RAG: nº de chunks hoje e metadados (state/demand_type/agency) | **PARCIAL** | [app/models/knowledge_catalog.py:62-105](app/models/knowledge_catalog.py#L62-L105) | Contagem de chunks é **dado de runtime**, não determinável por leitura estática do código neste clone. Schema `knowledge_catalog` tem metadados: `source_type`, `jurisdiction`, `uf`(2 chars), `agency`, `identifier`, `effective_date`, `extra_metadata`(JSONB). **Não há coluna `demand_type` no chunk.** `demand_type` vive em `LegislationDocument.demand_types` (array JSONB), usado por `search_legislation` ([app/services/legislation_service.py:161-164](app/services/legislation_service.py#L161-L164)). |
| 11 | `DemandType` (enum) — listar os valores e cobertura na base | **PARCIAL** | [app/models/process.py:32-44](app/models/process.py#L32-L44) | Enum tem **11 valores**: `car`, `retificacao_car`, `licenciamento`, `regularizacao_fundiaria`, `outorga`, `defesa`, `compensacao`, `exigencia_bancaria`, `prad`, `misto`, `nao_identificado`. Cobertura por valor na base regulatória depende de dados de runtime (`LegislationDocument.demand_types` por diploma) — não verificável estaticamente. O fallback de regras cobre 9 dos 11 explicitamente ([app/agents/legislacao.py:389-399](app/agents/legislacao.py#L389-L399)); `misto` e `nao_identificado` caem no default. |
| 12 | Lógica que ramifica fluxo por `demand_type` (`if`/`match`) — call sites | **PARCIAL** | múltiplos (abaixo) | Não há `match demand_type:` nem cadeia `if/elif` que ramifique a **estrutura** do fluxo. O `demand_type` é usado como **chave de lookup / filtro / guard**: `workflow_engine._find_template` ([app/services/workflow_engine.py:221-238](app/services/workflow_engine.py#L221-L238)), `checklist_engine` ([app/services/checklist_engine.py:226-230](app/services/checklist_engine.py#L226-L230)), `contract_generator.find_template_for_demand` ([app/services/contract_generator.py:193-206](app/services/contract_generator.py#L193-L206)), `search_legislation` filtro JSONB ([app/services/legislation_service.py:161](app/services/legislation_service.py#L161)), `legislation_monitor` ([app/services/legislation_monitor.py:174](app/services/legislation_monitor.py#L174)), `orcamento._estimate_by_rules` ([app/agents/orcamento.py:95](app/agents/orcamento.py#L95)). A diferenciação por demanda acontece via **dados** (WorkflowTemplate/ChecklistTemplate/ContractTemplate selecionados pelo `demand_type`), não via código ramificado. |

**Sobre o motor de workflow em si (contexto do item 2.2):** existe `WorkflowTemplate` + `workflow_engine.py` que aplica template de etapas por `demand_type` criando Tasks com dependências ([app/services/workflow_engine.py:58-104](app/services/workflow_engine.py#L58-L104)) e calcula status/progresso ([linha 107](app/services/workflow_engine.py#L107)). `_find_template` retorna `None` se não houver template para o `demand_type` → `apply_workflow_template` retorna lista vazia (sem trilha). Cobertura de templates por tipo é dado de runtime.

---

## Item 2.3 — Modelo de credenciais (logins de portais de órgão)

| # | O que checa | Status | Onde no código | Observação factual |
|---|---|---|---|---|
| 13 | Modelo `Credential`/`ClientCredential`/`PortalLogin` em `app/models/` | **FALTA** | `app/models/` (29 modelos listados) | Nenhum modelo de credencial. Lista de modelos não contém Credential/PortalLogin/ClientCredential. Busca por `credential`/`PortalLogin` no `app/` só retorna `allow_credentials=True` (CORS) em [app/main.py:122](app/main.py#L122). |
| 14 | Padrão de criptografia de segredos (Fernet/AES, encrypt/decrypt) e chave-mestra | **FALTA** | — | Nenhum uso de `cryptography.fernet`/`Fernet`/`AES`/`encrypt`/`decrypt` em `app/`. Não há chave-mestra para segredos por cliente. (Senhas de usuário usam hashing via `app/core/security`; isso é auth, não cofre de credenciais de terceiros.) |
| 15 | Onde vivem segredos hoje e padrão para "dado sensível por cliente" | **PARCIAL** | [app/core/config.py](app/core/config.py); `_env.example` | Segredos do **sistema** vivem em env vars / `settings` (SMTP, MinIO, LLM keys, RESEND, SECRET_KEY ≥32). **Não existe** padrão estabelecido para "segredo por cliente/tenant" — nada como cofre, criptografia em coluna ou tabela de credenciais por tenant. |
| 16 | Coluna/JSONB no Cliente que poderia abrigar credenciais hoje | **PARCIAL** | [app/models/client.py:41-46](app/models/client.py#L41-L46) | `Client` tem `notes` (Text), `extra_json` (Text, "JSON extra sem schema fixo") e `field_sources` (PortableJSON). Tecnicamente abrigariam credenciais em texto plano, mas **não há código que use esses campos para esse fim** — `extra_json` é genérico e sem schema. |
| 17 | Auditoria para acesso a campos sensíveis (AuditLog ouvindo leituras) | **FALTA** | [app/models/audit_log.py:8-19](app/models/audit_log.py#L8-L19) | `AuditLog` existe (`entity_type`, `action`, hash chain SHA-256), mas `action` é orientado a escrita — exemplos no próprio modelo: `'status_changed'`, `'created'`, `'updated'`. Não há ação de `read`/`view`/`access` nem auditoria de leitura de campo sensível. |

---

## O que ainda precisa existir para fechar cada item

### Item 2.1 — Ingestão real de WhatsApp e e-mail
- **FALTA** endpoint/webhook que receba payload de provider externo (nenhuma rota de inbound em `app/api/v1/`).
- **FALTA** lógica em `agent_atendimento` (ou camada anterior) para receber/parsear mensagem externa — hoje só consome `description` já presente no contexto.
- **PARCIAL** config de provider: existe SMTP de saída e Resend; **falta** config de canal inbound (WhatsApp/IMAP/Twilio/Evolution/Z-API/Mailgun/SendGrid).
- **FALTA** caminho de criação programática de `IntakeDraft` — única criação é o wizard manual autenticado.
- **PARCIAL** modelo de inbox: `CommunicationThread`/`Message` têm campos para canal externo (`channel`, `external_id`, `external_msg_id`), mas só são populados por endpoint manual com usuário interno; **falta** ingestão que os crie a partir de mensagem recebida.
- **FALTA** dependência de cliente WhatsApp / parser de e-mail no `requirements.txt`.

### Item 2.2 — Motor de workflow por tipo de demanda
- **EXISTE** o essencial: `agent_legislacao` com RAG + dump por metadados; `workflow_engine` aplicando `WorkflowTemplate` por `demand_type`; output `EnquadramentoRegulatorioContent` validado.
- **PARCIAL** filtro RAG por demanda: `knowledge_catalog` (chunks) não tem coluna `demand_type`; o `demand_type` entra só na string de query semântica. O filtro estruturado por `demand_type` existe apenas no nível de `LegislationDocument.demand_types` (dump por metadados), não no RAG vetorial.
- **PARCIAL** cobertura por tipo: os 11 valores do enum estão definidos, mas a cobertura efetiva (chunks/templates por `demand_type`) é dado de runtime e não foi verificável por leitura estática; `_find_template` sem match retorna trilha vazia.
- **PARCIAL** ramificação de fluxo: não há switch/branch por `demand_type`; a diferenciação é via seleção de templates (dados). Constatação, não lacuna funcional declarada.

### Item 2.3 — Modelo de credenciais (logins de portais de órgão)
- **FALTA** modelo dedicado (`Credential`/`PortalLogin`/`ClientCredential`).
- **FALTA** padrão de criptografia de segredos por cliente (sem Fernet/AES/encrypt/decrypt; sem chave-mestra para esse fim).
- **PARCIAL** armazenamento de segredo por cliente: só campos genéricos sem schema (`Client.notes`, `Client.extra_json`), não usados para credenciais.
- **FALTA** auditoria de leitura de campo sensível — `AuditLog` cobre escrita (`created`/`updated`/`status_changed`), sem ação de acesso/leitura.

---

*Auditoria gerada em modo read-only. Nenhuma alteração feita ao repositório ou banco além deste arquivo.*
